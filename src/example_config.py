# Configuration for Mercury

# Domain: Must contain the protocol, leading and trailing slashes, and be a valid domain name.
domain = ""  # "https://mercury.checksum.space/"

# MongoDB settings
mongo_settings = {
    "host": "",
    "user": "",
    "password": "",
    "db": "",
}

# Max file size in bytes
max_file_size = 50 * 1024 * 1024  # 50 MB (default)
